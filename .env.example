# Server Configuration
PORT=3000

# Authentication (Optional)
# If set, all /api endpoints will require 'Authorization: Bearer <API_KEY>' or 'apiKey=<API_KEY>' query param.
# Supports multiple keys via comma-separated string (e.g., key1,key2,key3).
API_KEY=
